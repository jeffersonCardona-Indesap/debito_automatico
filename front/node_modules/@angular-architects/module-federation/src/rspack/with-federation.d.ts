import { ModuleFederationConfig, RsbuildConfig } from '@rsbuild/core';
import { SkipList } from '../utils/skip-list';
export type FederationConfig = {
    options: FederationOptions;
    skip?: SkipList;
};
export type FederationOptions = Omit<ModuleFederationConfig['options'], 'name'> & {
    name?: string;
};
export declare function withFederation(config: FederationConfig): (rsbuildConfig: RsbuildConfig) => RsbuildConfig;
export declare function applyFederation(rsbuildConfig: RsbuildConfig, federationConfig: FederationConfig): RsbuildConfig;
