import { TipoMatriculaPipe } from './tipo-matricula.pipe';

describe('TipoMatriculaPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoMatriculaPipe();
    expect(pipe).toBeTruthy();
  });
});
